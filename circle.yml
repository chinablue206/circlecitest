machine:
  timezone:
    Asia/Tokyo
  java:
    version: openjdk7

general:
  artifacts:
    - "android/build/outputs/apk/android-debug.apk"

dependencies:
  pre:
    - chmod -x ./gradlew


test:
  post:
    - ./gradlew checkstyle
    - cp -r build/reports/* $CIRCLE_TEST_REPORTS
